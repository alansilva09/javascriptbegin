const doubleVelocity = (velocity) => {
  return velocity * 2
}

console.log(doubleVelocity(60))

// arrow por causa da setinha

// mais simples:

const doubleVelocity = (velocity) => velocity * 2

console.log(doubleVelocity(60))

// ou

const doubleVelocity = velocity => velocity * 2

console.log(doubleVelocity(60))